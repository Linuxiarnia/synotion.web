import React, { useState } from 'react';

export const NavBar = () => {

    type Page = 'Home' | ""

    const [currentPage: Page, setCurrentPage] = useState('Home');

    return(<>
    
    </>);
};
